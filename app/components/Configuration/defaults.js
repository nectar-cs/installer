const defaults = {
  verification: {
    failed: [
      "One or more command failed.",
      "The installer isn't yet smart enough to diagnose errors.",
      "Try running the command manually."
    ]
  }
};

export default defaults;
